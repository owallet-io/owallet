{
  "name": "@owallet/stores-core",
  "version": "0.12.130",
  "main": "build/index.js",
  "author": "owallet-io",
  "license": "Apache-2.0",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf node_modules; rm -rf build",
    "build": "tsc",
    "dev": "tsc -w",
    "test": "jest --passWithNoTests",
    "lint-test": "eslint \"src/**/*\" && prettier --check \"src/**/*\"",
    "lint-fix": "eslint --fix \"src/**/*\" && prettier --write \"src/**/*\""
  },
  "dependencies": {
    "@owallet/background": "*",
    "@owallet/common": "*",
    "@owallet/cosmos": "*",
    "@owallet/router": "*",
    "@owallet/simple-fetch": "*",
    "@owallet/stores": "*",
    "@owallet/types": "*",
    "buffer": "^6.0.3"
  },
  "peerDependencies": {
    "mobx": "^6",
    "mobx-utils": "^6"
  }
}
