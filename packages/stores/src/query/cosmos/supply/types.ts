export type SupplyTotal = {
  amount: {
    denom: string;
    amount: string;
  };
};

export type SupplyTotalStargate = {
  amount: {
    denom: string;
    amount: string;
  };
};

export type MintingInflation = {
  inflation: string;
};
