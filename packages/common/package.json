{
  "name": "@owallet/common",
  "version": "0.9.10",
  "main": "build/index.js",
  "author": "chainapsis",
  "license": "Apache-2.0",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf node_modules; rm -rf build",
    "build": "find build -name '*.d.ts' -type f -delete; tsc",
    "dev": "tsc -w",
    "test": "jest --passWithNoTests",
    "lint-test": "eslint \"src/**/*\" && prettier --check \"src/**/*\"",
    "lint-fix": "eslint --fix \"src/**/*\" && prettier --write \"src/**/*\""
  },
  "dependencies": {
    "@owallet/crypto": "^0.9.10",
    "buffer": "^6.0.3",
    "delay": "^4.4.0"
  }
}
